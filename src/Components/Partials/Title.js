import React from "react";

export const Title = ({ text }) => {
    return (
        <h2 className="text-blue-400 font-semibold text-5xl pb-[62px] text-center">
            {text}
        </h2>
    );
};
